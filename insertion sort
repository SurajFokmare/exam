#include <stdio.h>

int main(){
    int A[10] = {22,67,31,90,45,56,21,96,100,77};
    int n = 10;
    for(int i = 1; i < n; i++){
        int key = A[i];
        int j = i - 1;
        while(j >= 0 && A[j] > key){
            A[j+1] = A[j];
            j--;
        }
        A[j+1] = key;
    }
    printf("Sorted (Insertion): ");
    for(int i=0;i<n;i++) printf("%d ", A[i]);
    printf("\n");
    return 0;
}
